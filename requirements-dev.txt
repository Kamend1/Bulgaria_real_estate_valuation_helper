# Dev/test-only — not needed to run the app itself. See README's "Тестове"
# section. Layered on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1   # FastAPI's TestClient needs this
